import styled from "styled-components"
import img from "../../images/tenor.gif"

export const LayoutWrapper = styled.div`
  margin: 0 auto;
  min-height: 100vh;
  padding: 0;
  background-image: url(${img});
  background-repeat: repeat;
`
